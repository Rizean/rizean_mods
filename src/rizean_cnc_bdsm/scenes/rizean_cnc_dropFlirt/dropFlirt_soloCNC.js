module.exports = function dropFlirt_soloCNC({scene, Target}) {
    const {Player, none, $WHERE, narrative, option, random, WHEN} = scene
    // todo dropFlirt_soloCNC
    narrative("todo dropFlirt_soloCNC")
}